import "./style.scss";

import MenuMobile from "../../components/MenuMobile";

const Header = (props) => {
  return (
    <div className="header">
      <MenuMobile />
    </div>
  );
}

export default Header;
