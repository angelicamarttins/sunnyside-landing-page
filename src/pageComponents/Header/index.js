import "./style.scss";

import MenuMobile from "../../components/MenuMobile";

const Header = (props) => {
  return (
    <header className="header">
      <MenuMobile />
    </header>
  );
}

export default Header;
